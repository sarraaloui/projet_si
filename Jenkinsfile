pipeline{
    agent {
        docker:{image:'nginx'}
        
    }
        
        
        
    stages{
        stage('running build'){
            steps{
                sh 'nginx -v'}
            
        }}
}